#include <iostream>
using namespace std;

int main(){
    int Grades[5];
    int grade;
    int total = 0;
    int average;
    int len = sizeof(Grades)/sizeof(Grades[0]);

    for (int i = 0; i < len; i++){
        while (grade ){
            cout << "Grade: " << endl;
            cin >> grade;

        }
    }

    cout << "Welcome to the student grade manager!" << endl; 

    return 0;
}